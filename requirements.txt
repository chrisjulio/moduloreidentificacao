# ============================================================
# Core dependencies — Baseline de Reidentificação em Redes Sociais
# ============================================================
#
# Pinagem por piso (>=). Para reprodutibilidade estrita após o setup do
# ambiente, gere lockfile via:
#     pip freeze > requirements.lock.txt
#
# NOTA — pymetis (backend METIS para particionamento de grafos):
#   NÃO está listado aqui pois não instala via pip no Windows.
#   - Linux/macOS : pip install pymetis          (compila do fonte)
#   - Windows     : conda install -c conda-forge pymetis  (binário)
#   - Fallback    : backend Kernighan-Lin ativa automaticamente
#                   se pymetis estiver ausente (UserWarning emitido)
#   Setup completo: veja environment.yml e scripts/setup_conda_windows.ps1

# Grafos
# NOTA: pinado em >=3.5 — a semântica de weisfeiler_lehman_graph_hash
# para grafos sem atributos mudou na v3.5, garantindo consistência de
# hashes entre ambientes e reprodutibilidade dos testes de agrupamento.
networkx>=3.5

# Numérico e estatístico (KS-test em scipy.stats.ks_2samp)
numpy>=2.0
scipy>=1.14

# Visualização e tabelas
matplotlib>=3.9
pandas>=2.2

# Configuração e UX
pyyaml>=6.0
tqdm>=4.66
